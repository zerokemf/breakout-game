import {test,expect} from '@playwright/test';
test.use({launchOptions:{args:['--unsafely-treat-insecure-origin-as-secure=http://192.168.1.106:7676']}});
const base=process.env.ROMM_URL||'http://192.168.1.106:7676';
test('real RomM ultrawide embed uses height, fullscreen and other games unchanged',async({page})=>{await page.setViewportSize({width:3440,height:1440});await page.goto(base+'/#online-games/breakout');const iframe=page.locator('iframe.wo-frame');await expect(iframe).toBeVisible({timeout:20000});const frame=page.frameLocator('iframe.wo-frame');await frame.locator('[data-action="play"]').click();await frame.locator('[data-action="launch"]').click();const canvas=await frame.locator('canvas').boundingBox();console.log('normal',canvas);expect(canvas.width).toBeGreaterThan(1750);expect(canvas.height).toBeGreaterThan(980);await page.screenshot({path:'tests/artifacts/romm-normal.png'});await page.locator('.wo-bar button').click();await expect(page.locator('#willie-online')).toHaveJSProperty('offsetWidth',3440);const full=await frame.locator('canvas').boundingBox();console.log('fullscreen',full);expect(full.height).toBeGreaterThan(1200);expect(full.y+full.height).toBeLessThanOrEqual(1440);await page.screenshot({path:'tests/artifacts/romm-fullscreen.png'});await page.evaluate(()=>document.exitFullscreen());await page.goto(base+'/#online-games/tetris');await expect(page.locator('#willie-online')).toHaveAttribute('data-game','tetris');expect((await page.locator('#willie-online').boundingBox()).width).toBeLessThan(1500);});
