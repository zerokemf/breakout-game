import test from 'node:test';import assert from 'node:assert/strict';import {Renderer} from '../js/renderer.js';
test('life-bound width displays readable state and never Infinity seconds',()=>{const texts=[];const ctx=new Proxy({createLinearGradient:()=>({addColorStop(){}}),createRadialGradient:()=>({addColorStop(){}}),fillText:t=>texts.push(t)},{get:(o,k)=>k in o?o[k]:()=>{},set:(o,k,v)=>(o[k]=v,true)});const r=new Renderer({getContext:()=>ctx},{render(){}});r.render({paddle:{x:500,y:658,width:255,height:18},effects:{extend:Infinity,laser:5}});assert.ok(!texts.some(t=>t.includes('本命有效')));assert.ok(!texts.some(t=>t.includes('Infinity')));assert.ok(!texts.some(t=>t.includes('5.0秒')));});
