import {test,expect} from '@playwright/test';
const base=process.env.TEST_BASE_URL||'http://127.0.0.1:8765';
for(const [width,height] of [[320,700],[390,844],[844,390],[768,1024],[1440,1000]])test(`phase2 visual ${width}x${height}`,async({page})=>{await page.setViewportSize({width,height});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(base);await page.getByRole('button',{name:'PLAY',exact:true}).click();await page.getByRole('button',{name:'LAUNCH',exact:true}).click();await page.evaluate(async()=>{const {game}=await import('./js/main.js?v=2a');game.applyPowerup('extend');game.applyPowerup('fire');game.applyPowerup('laser');game.applyPowerup('multi');});await page.waitForTimeout(800);await page.screenshot({path:`tests/artifacts/phase2-play-${width}.png`});expect(errors).toEqual([]);const c=await page.locator('canvas').boundingBox();expect(c.y+c.height).toBeLessThanOrEqual(height);await page.getByRole('button',{name:'Pause game'}).click();await page.getByRole('button',{name:'MAIN MENU',exact:true}).click();await page.getByRole('button',{name:'SETTINGS',exact:true}).click();await expect(page.getByText('LASER PADDLE',{exact:false})).toBeVisible();await page.getByRole('button',{name:'MAIN MENU',exact:true}).scrollIntoViewIfNeeded();await page.screenshot({path:`tests/artifacts/phase2-settings-${width}.png`});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);});
